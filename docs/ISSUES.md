# ルートページ (index.html) 課題管理

対象: `/` (index.html) および関連バックエンド (app.py)

## バックエンド問題

### B-1: /debug_summary のSPデータ処理がexceptブロック内に誤配置
- **ファイル**: app.py:348-391
- **状態**: 未着手
- **詳細**: SPデータの統合処理（special_attack, sp_info, power, descriptions）がtry/exceptのexceptブロック内にインデントされている。正常パスではSPデータが一切処理されない。
- **影響**: カード詳細モーダルでSPアタック情報が表示されない可能性（index.htmlは/api/card/<number>経由だが、debug_summaryルートに依存する画面あり）

### B-2: /fetch_cards が非debugモードでOCRデータ空
- **ファイル**: app.py:453
- **状態**: 未着手
- **詳細**: `ocr_results/` ディレクトリが存在しない（`ocr_results_debug/` のみ）。debug=1なしで呼ばれるとOCRデータが空になる。
- **影響**: index.htmlは `/api/card_index` を使うため直接影響は小さいが、`/post_deck` で投稿されたデッキの復元時に影響する可能性あり

## フロントエンド問題 (index.html)

### F-1: decks.htmlからのデッキインポートが機能しない
- **ファイル**: index.html (autoLoad)
- **状態**: 未着手
- **詳細**: decks.htmlは `gundamDecks_import` キーでlocalStorageに保存してリダイレクトするが、index.htmlの `_autoLoad()` はそのキーを読みに行かない。
- **影響**: デッキ一覧ページからのインポート操作が無効

### F-2: PLカードのカテゴリ表示の一貫性
- **ファイル**: index.html (CardBrowser, DeckAnalysis)
- **状態**: 確認中
- **詳細**: PLカードのカテゴリ（殲滅/制圧/防衛）がバックエンドの補正に依存しており、OCRデータの構造によって表示が不安定になるケースがある。
- **影響**: カードブラウザのカテゴリフィルタで正しく絞り込めない場合がある

### F-3: リンクアビリティのバフ計算がカード個別で実行される
- **ファイル**: index.html:1527-1562 (getLinkBuffs)
- **状態**: 確認中
- **詳細**: `getLinkBuffs` がカード単体のlink_abilitiesをベースに計算しているが、デッキ全体でのリンク発動判定と統合されている。計算ロジックがindex.htmlとsummary.htmlで異なる実装（小アップ=1.1倍 vs +10）。
- **影響**: ユニットステータスのバフ表示値が不正確な可能性

## 確認済み（問題なし）

- カードインデックス読み込み (`/api/card_index`) → 正常動作
- カード詳細取得 (`/api/card/<number>`) → 正常動作
- デッキ保存/読込 (localStorage `gab_decks`) → 正常動作
- 自動保存/復元 (localStorage `gab_autosave`) → 正常動作
- URLハッシュによるデッキ共有 → 正常動作
- Undo/Redo → 正常動作
- 仮想スクロール → 正常動作
- キーボードショートカット → 正常動作
- デッキコードのコピー/インポート → 正常動作
- カード詳細モーダル → 正常動作
- 地形適性表示（右パネル） → 正常動作
- コスト計算 → 正常動作

## 完了済み

### F-4: MSスロットにPLカード（またはその逆）をセットできてしまう
- **ファイル**: index.html (_selectCard)
- **状態**: 完了
- **対応**: アクティブスロットのタイプとカードタイプの不一致時にエラー表示してブロック。スロット未選択時は同タイプの空きスロットが5枚埋まっている場合にメッセージ表示。

### B-3: OCRデータが145枚(FQ01/FQ02)のみ、残り2387枚が欠損
- **ファイル**: ocr_results_debug/
- **状態**: 対応中（Claude CodeによるOCR）
- **詳細**: 全2532枚中、OCRデータがあるのはFQ01(71枚)+FQ02(74枚)=145枚のみ。残りはコスト・ステータス等が一切表示されない。
- **影響**: 大半のカードでコスト合計=0、ステータスバー=0になる
- **対応方針**: Claude Codeで画像を読み取り、既存フォーマットのJSONを生成して保存

## 進捗ログ

- 2026-02-22: 全ページ調査完了。ルートページ(index.html)のみを対象に課題を整理。
- 2026-02-22: F-4 MSとPLのスロット制限を実装。各タイプ最大5枚のバリデーション追加。
- 2026-02-22: B-3 Claude CodeでAB01-001(ガンダム)のOCRテスト成功。表裏両面から全データを正確に抽出。
